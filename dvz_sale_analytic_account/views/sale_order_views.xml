<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ================================================================
         Adds the restored header-level Analytic Account field to the
         Sales Order / Quotation form, placed right after Salesperson in
         the "Other Info" tab's Sales column - matching where the
         equivalent field sat on the Odoo 16 form.
         NOTE: this xpath targets field name="user_id" (Salesperson),
         which has been a long-standing stable field on sale.order across
         many Odoo versions. If your v19 build has renamed/moved it,
         this xpath will fail with a clear "cannot be located in parent
         view" error naming the exact issue - safe to re-target from there.
         ================================================================ -->
    <record id="dvz_sale_order_form_analytic_account" model="ir.ui.view">
        <field name="name">sale.order.form.dvz.analytic.account</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='journal_id']" position="before">
                <field name="analytic_account_id"/>
            </xpath>
        </field>
    </record>

</odoo>
